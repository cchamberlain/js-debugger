"use 6to5";

module.exports = function f(src, dest) {
  return src.subscribe(
    data => {
      dest.onNext(data);
    },
    () => {
      dest.onCompleted();
    },
    err => dest.onError(err)
  );
};
