"use 6to5";

/**
 * one way data flow
 * Model->View->Action->Void
 */
module.exports = function bind(model, view, action) {
  model.observe(action);
  view.observe(model);
  action.observe(view);
};
